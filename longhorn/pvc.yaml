apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: home-assistant
  name: longhorn-volv-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
